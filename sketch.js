var canvas,backimg;
var gamestate = 0;
var playercount,database,form,player,game;

function setup(){
    canvas = createCanvas(400,400);
    database = firebase.database();
}
function draw(){
    
}
